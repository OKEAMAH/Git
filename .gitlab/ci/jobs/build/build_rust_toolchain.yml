include: .gitlab/ci/jobs/build/common.yml

oc.image:rust-toolchain:
  extends:
    - .image_template__docker
    - .docker_auth_template
  stage: build
  needs: [trigger]
  variables:
    # These image are not built for external use.
    CI_DOCKER_HUB: "false"
  script:
    - ./scripts/ci/docker_rust_toolchain_build.sh
