test_kernels:
  extends:
    - .kernels_template
    - .rules__octez_changes
  stage: test
  needs: [build_kernels]
  dependencies: [build_kernels]
  script:
    - make -f kernels.mk check
    - make -f kernels.mk test
  rules:
    - changes:
        - src/kernel_*/**/*
        - src/risc_v/**/*
