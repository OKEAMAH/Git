test_kernels:
  extends:
    - .oc.kernels_template
    - .rules__octez_changes
  stage: test
  needs: [oc.build_kernels]
  dependencies: [oc.build_kernels]
  script:
    - make -f kernels.mk check
    - make -f kernels.mk test
  rules:
    - changes:
        - src/kernel_*/**/*
        - src/risc_v/**/*
