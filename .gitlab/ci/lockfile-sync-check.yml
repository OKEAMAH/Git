---

lockfile_sync_check:
  extends: .build_template
  needs: []
  variables:
    ARCH: "x86_64"
  script:
    - make lock
    - diff tezos.opam.locked /home/tezos/tezos.opam.locked
  only:
    changes:
      - scripts/version.sh
      - tezos.opam.locked
